# LearnLens Frontend

Next.js frontend for LearnLens, deployed on Vercel.

## Environment Variables
- `BACKEND_URL` — URL of the Flask backend (deployed on Render), e.g. `https://learnlens-mxwi.onrender.com`

## Local development
```bash
npm install
npm run dev
